CLI Arguments Parser — README

Этот скрипт показывает как PHP получает и интерпретирует аргументы командной строки.

Он не делает «магии», а пошагово превращает массив строк → структурированный ввод, с которым уже можно работать в коде.

⸻

🎯 Главная идея (если понять только одно)

CLI-парсер — это переводчик.
Он переводит:

["migrate", "users", "--env=prod", "-v"]

в:

$command = "migrate";
$args    = ["users"];
$options = [
    "env" => "prod",
    "v"   => true,
];



PHP сам не знает, что такое --env или -v.
Смысл этим строкам придаёт только наш код.

⸻

📥 Что PHP даёт нам из коробки

При запуске:

php cli.php migrate users --env=prod -v

PHP создаёт две переменные:

$argc // количество аргументов
$argv // массив аргументов

Содержимое $argv ВСЕГДА такое:

[
  "cli.php",        // имя файла (ВСЕГДА первый элемент)
  "migrate",
  "users",
  "--env=prod",
  "-v"
]

📌 PHP больше ничего не делает.
Никаких команд, опций и флагов для него не существует.

⸻

🧹 Шаг 1. Убираем имя файла

Имя файла нам не нужно:

array_shift($argv);

Теперь $argv:

[
  "migrate",
  "users",
  "--env=prod",
  "-v"
]


⸻

🧠 Модель, которую использует парсер

Скрипт договорился сам с собой о таких правилах:
	1.	Первое слово без - — команда
	2.	Всё, что начинается с -- — длинные опции
	3.	Всё, что начинается с - — короткие флаги
	4.	Всё остальное — позиционные аргументы

Это не стандарт PHP — это выбранная нами модель.

⸻

🧩 Типы аргументов и как они распознаются

1️⃣ Команда

Принцип:

Первая строка, которая НЕ начинается с -

migrate

В коде:

if ($command === null) {
    $command = $arg;
}

Результат:

$command === "migrate";


⸻

2️⃣ Позиционные аргументы

Принцип:

Всё, что не команда и не опция

users products

В коде:

$args[] = $arg;

Результат:

$args === ["users", "products"];

📌 Порядок важен — это данные для команды.

⸻

3️⃣ Длинные опции (--key)

Формат --key=value

--env=prod

Принцип:
	•	убираем --
	•	делим по =

Результат:

$options['env'] = 'prod';


⸻

Формат --key value

--env prod

Принцип:
	•	если следующее значение не начинается с - — это значение опции

Результат:

$options['env'] = 'prod';


⸻

Формат --flag

--force

Принцип:
	•	нет значения → булев флаг

Результат:

$options['force'] = true;


⸻

4️⃣ Короткие флаги (-abc)

-vf

Принцип:
	•	убираем -
	•	разбиваем строку на символы

Результат:

$options['v'] = true;
$options['f'] = true;

📌 Это копия поведения Unix-утилит (ls -la).

⸻

🔄 Как работает цикл парсинга

Парсер по одному берёт элементы $argv:

while (($arg = array_shift($argv)) !== null) {
    // анализируем $arg
}

Почему так:
	•	мы контролируем порядок
	•	можем “заглядывать” в следующий аргумент
	•	можем безопасно забирать значения

⸻

🧠 Почему результат именно такой структуры

$command // что делать
$args    // над чем делать
$options // как именно делать

Это универсальная модель:

ДЕЙСТВИЕ + ДАННЫЕ + ПОВЕДЕНИЕ

Она используется в:
	•	git
	•	docker
	•	composer
	•	symfony console

⸻

🚫 Что этот парсер ОСОЗНАННО не делает

Чтобы не скрывать механику, он не поддерживает:
	•	-f value (короткие опции со значением)
	•	-- (end of options)
	•	кавычки ("hello world")
	•	автоматический --help

Это сделано специально для обучения.

⸻

🧪 Как проверить себя

Ниже приведены тест-кейсы, которые можно запускать вручную.
Их цель — не проверить «правильно/неправильно», а понять, почему результат именно такой.

⸻

🧪 Тест-кейсы (ручное тестирование)

Тест 1. Минимальный запуск

php cli.php hello

Ожидание:
	•	command = hello
	•	args = []
	•	options = []

Смысл:
Проверка базового правила: первое слово без - — команда.

⸻

Тест 2. Команда + позиционные аргументы

php cli.php greet Nick John

Ожидание:
	•	command = greet
	•	args = ["Nick", "John"]
	•	options = []

Смысл:
Порядок аргументов важен, имена не имеют значения.

⸻

Тест 3. Длинная опция с =

php cli.php migrate users --env=prod

Ожидание:
	•	command = migrate
	•	args = ["users"]
	•	options = ["env" => "prod"]

Смысл:
Опция может содержать значение в одном токене.

⸻

Тест 4. Длинная опция со значением через пробел

php cli.php migrate users --env prod

Ожидание:
	•	options = ["env" => "prod"]

Смысл:
Парсер смотрит на следующий аргумент и решает, значение это или нет.

⸻

Тест 5. Булевый флаг

php cli.php migrate users --force

Ожидание:
	•	options = ["force" => true]

Смысл:
Отсутствие значения трактуется как включённое поведение.

⸻

Тест 6. Короткий флаг

php cli.php greet Nick -v

Ожидание:
	•	options = ["v" => true]

Смысл:
Короткие флаги — это всегда boolean.

⸻

Тест 7. Объединённые короткие флаги

php cli.php task run -vf

Ожидание:
	•	options = ["v" => true, "f" => true]

Смысл:
-vf эквивалентно -v -f.

⸻

Тест 8. Смешанный ввод

php cli.php deploy app --env=prod --force -v

Ожидание:
	•	command = deploy
	•	args = ["app"]
	•	options = env=prod, force=true, v=true

Смысл:
Типичный реальный CLI-вызов.

⸻

Тест 9. Порядок опций не важен

php cli.php deploy --force app --env=prod

Ожидание:
	•	command = deploy
	•	args = ["app"]
	•	options = force=true, env=prod

Смысл:
Опции можно переставлять местами.

⸻

Тест 10. Неизвестная команда

php cli.php unknown arg1 arg2

Ожидание:
	•	command = unknown
	•	args = ["arg1", "arg2"]

Смысл:
Парсер ничего не знает о бизнес-логике, он только разбирает ввод.

⸻

Тест 11. Только опции (без команды)

php cli.php --env=dev -v

Ожидание:
	•	command = null
	•	options = env=dev, v=true

Смысл:
Команда — это соглашение, а не требование PHP.

⸻

Тест 12. Ошибка проектирования (показательная)

php cli.php greet --env -v

Ожидание:
	•	options = ["env" => true, "v" => true]

Смысл:
Без значения --env превращается в флаг.
Это показывает, почему нужна валидация аргументов.

⸻

Запусти:

php cli.php greet Nick --yell -v

И посмотри вывод var_dump:

Ты должен чётко увидеть:
	•	где команда
	•	где аргументы
	•	где опции

Если это видно — парсер понят.

⸻

🧩 Главное, что нужно вынести

CLI-парсер — это набор правил, а не встроенная функция PHP.

Ты:
	•	сам выбираешь правила
	•	сам решаешь, что считать командой
	•	сам определяешь формат аргументов

Этот скрипт — минимальный фундамент для любых CLI-инструментов на PHP.

⸻

🔜 Куда идти дальше
	1.	Валидация аргументов
	2.	--help
	3.	ООП-версия парсера
	4.	Сравнение с symfony/console

Если хочешь — следующий шаг сделаем ещё более пошаговым, прямо с трассировкой выполнения.